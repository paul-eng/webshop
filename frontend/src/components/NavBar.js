import React from "react";
import "../styles/NavBar.css";

const NavBar = (props) => {
  return (
    <div className="NavBar">
        <h1>Restful Goods</h1>
    </div>
  );
};

export default NavBar;